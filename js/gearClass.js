var Gear = function () {
	this.teethAngle = (Math.PI*2)/12;
	this.angle = 0;
	this.teethWidth = Math.sin(teethAngle/2)*48;
	this.numOfTeeth = numOfTeeth;
	this.radius = radius;
	this.x = 100 + radius * Math.cos(angle);
	this.y = 100 + radius * Math.sin(angle);;
};
Gear.prototype.createGear = function{
	
}